import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.AlSanaa.app',
  appName: 'Al Sanaa',
  webDir: 'dist',
  server: {
    url: 'https://alsanaa.alsanact.com',
  }
};

export default config;
